<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lael.infra.modules.code.CodeMapper">

    <resultMap id="resultMapObj" type="com.lael.infra.modules.code.Code"></resultMap>
	
	<select id="selectList" resultMap="resultMapObj">
		SELECT
			b.*
			,a.ccgSeq
		    ,a.name
		FROM 
			code b
		JOIN codeGroup a on a.ccgSeq = b.ccgSeq
		WHERE 1=1
		<!-- AND  b.example = 0 -->
			<if test="cdDelNy != null and !cdDelNy.equals('')">AND cdDelNy = #{cdDelNy}</if>
			<choose>
				<when test="shOption == 1">AND name = #{shValue}</when>
				<when test="shOption == 2">AND cdSeq = #{shValue}</when>
				<when test="shOption == 3">AND codeName LIKE CONCAT('%',#{shValue},'%')</when>
				<when test="shOption == 4">AND codeOrder LIKE CONCAT('%',#{shValue},'%')</when>
				<otherwise></otherwise>
			</choose>
			ORDER BY cdSeq DESC
	</select>
	
	<insert id="insert">
		INSERT INTO code (
			codeName
			,ccgSeq
		)
		VALUES (
			#{codeName}
			,#{ccgSeq}
		)
		
		<selectKey resultType="String" keyProperty="cdSeq" order="AFTER">
			SELECT last_insert_id()
		</selectKey>
	</insert>
	
<!-- 	<update id=""></update>
	<delete id=""></delete> -->

</mapper>	
